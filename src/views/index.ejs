<!DOCTYPE html>
<html lang="ja">
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <header>
      <h1><%= title %></h1>
    </header>
    <main>
      <table>
        <% const tableHead = ["id", "名前", "名前（かな）", "学年", "身長(cm)", "チーム", "担当", "リーダー", "ニックネーム"]; %>
        <% tableHead.forEach((item) => { %>
          <th><%= item %></th>
        <% }); %>
        <% content.forEach((stu) => { %>
          <tr>
            <td><%= stu.id %></td>
            <td><%= stu.name %></td>
            <td><%= stu.name_kana %></td>
            <td><%= stu.year %></td>
            <td><%= stu.height %></td>
            <td><%= stu.team %></td>
            <td><%= stu.role %></td>
            <td><%= stu.leader ? "○" : "" %></td>
            <td><%= stu.nickname %></td>
          </tr>
        <% }); %>
      </table>
    </main>
  </body>
</html>
