<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <title><%= title %></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <header>
      <h1 class="display-4"><%= title %></h1>
    </header>
    <main>
      <table class="table">
        <thead class="thead-light">
          <% const tableHead = ["id", "名前", "名前（かな）", "学年", "身長(cm)", "チーム", "担当", "リーダー", "ニックネーム"]; %>
          <% tableHead.forEach((item) => { %>
            <th><%= item %></th>
          <% }); %>
        </thead>
        <% content.forEach((stu) => { %>
          <tr>
            <td><%= stu.id %></td>
            <td><%= stu.name %></td>
            <td><%= stu.name_kana %></td>
            <td class="text-center"><%= stu.year %></td>
            <td class="text-center"><%= stu.height %></td>
            <td><%= stu.team %></td>
            <td><%= stu.role %></td>
            <td class="text-center"><%= stu.leader ? "○" : "" %></td>
            <td><%= stu.nickname %></td>
            <td class="border border-0">
              <a class="btn btn-secondary btn-sm" href="/edit?id=<%= stu.id %>">更新</a>
            </td>
            <td class="border border-0">
              <a class="btn btn-danger btn-sm" href="/delete?id=<%= stu.id %>">削除</a>
            </td>
          </tr>
        <% }); %>
      </table>
      <div id="add">
        <a class="btn btn-primary" href="/add" role="button">追加</a>
      </div>
    </main>
  </body>
</html>
